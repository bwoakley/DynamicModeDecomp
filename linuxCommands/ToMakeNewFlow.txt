%%%%%To make a new flow:

When changing cases, there are 3-5 places to update:
-1) Editing diablo.f
O) In input.dat when chosing case.
1) When copying to Case/name, (2 changes, see below).
2) When extracting, in res_to_dat, (1 change).
3) When post processing, in Matlab code (1 change).

If edited diablo.f, inside code0:
	rm -f diablo; rm *.o; make;
Produce data inside case1:
	rm -f diablo.saved*; rm -f diablo_th01*; rm -f pos*; rm -f Thi; ../diablo;
Copy data to Cases, from inside case1: (change case2 in 1 place, Lin5 in 2 places)
	cd ..; cd ..; cd Cases/; rm -f Line/*; cd ..; cd code0/; cp -a case1/. ../Cases/Line/
Extract. If choosing new folder in Cases, edit res_to_dat to find.
If edited res_to_dat, inside extract:
	rm res_to_dat; make; ./res_to_dat;

%%%%%If gedit gets stuck on `Failed to register: Timeout was reached'
Then do:
killall gedit
	
%%%%%Changing grid:
Besides changing grid_def in case0 and case2, need to create a new grid in: case2/diablo.start
Prof Tang gave a 512 grid diablo.start
You can create this grid by taking the Fourier transform, adding some extra coeff of 0, then taking the inverse Fourier trans. 
The spacial function will be defined on a larger grid.

%%%%%Transfering files from local Windows to remote Linux:
Open command prompt in Windows. 
Do:
pscp -P 2200 WindowsFile bwoakley@mathf2.la.asu.edu:Destination
Ex:
pscp -P 2200 C:\Users\boakl\Downloads\case2.tar bwoakley@mathf2.la.asu.edu:Documents

%%%%%When done on Linux server:
	
git add .  %don't need this line every time, only when adding new file.
git commit -a -m 'Your message here:'
git push origin main

git pull https://gitlab.com/boakley/mixingandlyapexp.git


